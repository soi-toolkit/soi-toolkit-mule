package ${javaPackage}.${lowercaseJavaService}.inbound;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

public class ${capitalizedJavaService}TestReceiver {

	private static final Logger log = LoggerFactory.getLogger(${capitalizedJavaService}TestReceiver.class);

	public void process(Object message) {
		log.info("${capitalizedJavaService}TestReceiver received the message: {}", message);
	}
}
