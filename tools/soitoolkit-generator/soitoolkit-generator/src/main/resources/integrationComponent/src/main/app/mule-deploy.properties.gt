# Mule Application Deployment Descriptor
redeployment.enabled=true
encoding=UTF-8
<% if (jdbc) { %>
config.resources=${artifactId}-config.xml,${artifactId}-common.xml,${artifactId}-jdbc-connector.xml
<% } else { %>  
config.resources=${artifactId}-config.xml,${artifactId}-common.xml
<% } %>  
domain=default
