package ${javaPackage}.${lowercaseService};

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

public class ${capitalizedService}TestReceiver {

	private static final Logger log = LoggerFactory.getLogger(${capitalizedService}TestReceiver.class);

	public void process(Object message) {
		log.info("${capitalizedService}TestReceiver received the message: {}", message);
	}
}
